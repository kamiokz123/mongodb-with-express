<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit chat</title>
</head>
<body>
    <p>Editing chat of id : <%= chat._id %></p>
    <br>
    <p>this message was sent from : <b><%= chat.from %></b> to : <b><%= chat.to %></b></p>
    <br>

    <form method="post" action="/chats/<%= chat._id %>/edit?_method=put">
        <textarea name="msg" rows="6" cols="20"><%= chat.msg %></textarea> <br>
        <button>Edit</button>
    </form>
</body>
</html>